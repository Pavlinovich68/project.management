'use client'
import React, {useRef, useState, useEffect} from "react";

const RoadmapRow = ({year, project_id}:{year: number, project_id: number}) => {
   return (
      <React.Fragment>

      </React.Fragment>
   );
};

export default RoadmapRow;
